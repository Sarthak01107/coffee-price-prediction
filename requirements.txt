Flask
pandas
numpy
prophet # Replaced statsmodels with prophet
scikit-learn # For train_test_split (if used)
matplotlib # For plotting in model training script (if needed for local testing)
joblib # For saving/loading the model
gunicorn
